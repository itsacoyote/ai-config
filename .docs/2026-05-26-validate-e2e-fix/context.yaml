# context.yaml — feature workflow context
# Created by the /spec skill. Read and updated by each agent before handing off.
# To add new fields, extend this template and update agent-context/SKILL.md.

feature:
  name: "Fix validate agent to run and fix e2e tests before PR is ready for review"
  short_name: "validate-e2e-fix"
  folder: ".docs/2026-05-26-validate-e2e-fix"
  date: "2026-05-26"
  branch: "feature/validate-e2e-fix"
  base_branch: "main"

workflow:
  current_step: implement
  completed_steps: [define, research, plan]
  checkpoint: "Completed all 5 tasks. All changes committed: qa-reviewer.md (e2e execution, fix loop, three-state verdict, evidence gate, non-negotiables), validate/SKILL.md (Round 2 e2e responsibility, green-suite gate, completion bullet), validate.md (E2E Test Run template section, e2e-failure escalation block), vocabulary consistency pass."
  escalated: false
  escalation_reason: ""

artifacts: []

recommended_skills:
  - skill: verify-coherence
    invoke_when: "After Tasks 1–4 are committed, before running Task 5, to confirm the three files use identical verdict-state vocabulary, green-suite phrasing, and fix-loop cap wording."

output_artifacts: []

documentation_created: []
