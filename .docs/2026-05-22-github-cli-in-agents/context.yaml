# context.yaml — feature workflow context
# Created by the /spec skill. Read and updated by each agent before handing off.
# To add new fields, extend this template and update agent-context/SKILL.md.

feature:
  name: "Audit agents and skills for git commands conflicting with github-tool-preference"
  short_name: "github-cli-in-agents"
  folder: ".docs/2026-05-22-github-cli-in-agents"
  date: "2026-05-22"
  branch: "feature/github-cli-in-agents"
  base_branch: "main"

workflow:
  current_step: document
  completed_steps: [define, research, plan, implement, validate]
  checkpoint: ""
  escalated: false
  escalation_reason: ""

artifacts: []

recommended_skills:
  - skill: verify-coherence
    invoke_when: "After Task 8 commits, before handing off to Validate — confirms all seven agent frontmatter blocks share the same shape and that `qa-reviewer.md` is the sole remaining `mcpServers` declarant."

output_artifacts: []

documentation_created: []
