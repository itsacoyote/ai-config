# context.yaml — feature workflow context
# Created by the /spec skill. Read and updated by each agent before handing off.
# To add new fields, extend this template and update agent-context/SKILL.md.

feature:
  name: "Sync Skill"
  short_name: "sync-skill"
  folder: ".docs/2026-06-01-sync-skill"
  date: "2026-06-01"
  branch: "feature/sync-skill"
  base_branch: "main"

workflow:
  current_step: implement
  completed_steps:
    - define
    - research
    - plan
  checkpoint: "Completed all tasks (1 and 2 of 2). Both commits landed. Ready for Validate."
  escalated: false
  escalation_reason: ""
  summary: |
    Implement landed both tasks in two commits against two files. The diff is
    entirely additive for the skill file and a net +4 lines for README.md. No
    existing files were modified beyond README.md, and no settings or agent files
    were touched.

    Task 1 created `.claude/skills/sync/SKILL.md` (344 lines, committed as
    `feat(skills): add sync skill for pre-feature local refresh`). The file has
    the five-field frontmatter block — `name: sync`, `description`, `disable-model-
    invocation: true`, and the full enumerated `allowed-tools` list (20 `Bash(<cmd> *)`
    patterns plus `Read`) — followed by 13 prose sections in the exact ordering the
    plan required: What this does, Preflight, Branch switch and pull, Change summary,
    Environment refresh (package managers, migrations, .env diff, Docker), Project-
    specific instructions, Final summary, Errors and exits, and What this skill will not
    do. The three verbatim blocks from the plan's Shared wording section — the dirty-tree
    two-option prompt, the per-ecosystem install confirmation prompt, and the eight-section
    final summary template with `═══` borders and numeric `1.`–`8.` prefixes — were copied
    without alteration. `Skill(github-tool-preference)` appears at exactly two seams
    inside `## Branch switch and pull`: once before `git fetch origin` and once before
    `git pull --ff-only origin <main>`. No broad `Bash(*)` pattern was used.

    Task 2 made four targeted edits to README.md (committed as `docs(readme): document
    /sync skill alongside /pr-review`): the `### PR review skill` heading was renamed to
    `### Standalone skills`; a `/sync` row was inserted above `/pr-review` in the table
    (alphabetical); the Usage line was updated to cover both `/sync` (no arguments) and
    `/pr-review <pr-number>`; and `sync/` was added to the file-reference block between
    `spec/` and `analyze-code/` with a matching comment.

    No deviations from the plan. The `sync/` insertion in the file-reference block lands
    after `spec/` (line 314) — consistent with the plan's "alphabetically between spec/
    and validate/" intent, since validate/ appears earlier in the list in pipeline order
    while the utility skills after it are loosely grouped.

    For Validate: the primary verification surface is SKILL.md prose fidelity — confirm
    the three verbatim blocks match the plan's Shared wording exactly (em-dashes U+2014,
    numeric section prefixes, `═══` border characters). Check that the `Skill(github-tool-
    preference)` seams are at the right positions inside `## Branch switch and pull` and
    not elsewhere. Verify README has no stray instances of the old `### PR review skill`
    heading. The skill has no tests and no runtime code — there is nothing to unit-test;
    all acceptance criteria from `1_spec.md` are structural/prose checks against the
    two changed files.

artifacts: []

recommended_skills:
  - skill: verify-coherence
    invoke_when: "After both tasks land, before the Validate step — confirm SKILL.md's structure, frontmatter, and prompt patterns mirror `pr-review/SKILL.md`, and that the README edits are internally consistent (heading rename matches the table caption, file-reference block matches the new directory, alphabetical placement is correct)."

output_artifacts: []

documentation_created: []
