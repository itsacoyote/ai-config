# context.yaml — feature workflow context
# Created by the /spec skill. Read and updated by each agent before handing off.
# To add new fields, extend this template and update agent-context/SKILL.md.

feature:
  name: "Sync Skill"
  short_name: "sync-skill"
  folder: ".docs/2026-06-01-sync-skill"
  date: "2026-06-01"
  branch: "feature/sync-skill"
  base_branch: "main"

workflow:
  current_step: document
  completed_steps:
    - define
    - research
    - plan
    - implement
    - validate
  checkpoint: "Completed all tasks (1 and 2 of 2). Both commits landed. Ready for Validate."
  escalated: false
  escalation_reason: ""
  summary: |
    Validate passed with one fix applied during senior code review and a clean QA pass
    on first iteration. The validation report is at `.docs/2026-06-01-sync-skill/4_validate.md`.

    Senior code review (2 iterations): One finding was raised and fixed. The first
    `Skill(github-tool-preference)` seam inside `## Branch switch and pull` was placed
    after `git checkout <main>` rather than immediately before `git fetch origin` as the
    plan specified. The `git rev-parse <main>` (pre-pull SHA capture) is a local operation
    and should not be gated by the tool-preference check; the seam was moved to appear
    after `git rev-parse <main>` and immediately before `git fetch origin`. Fixed in commit
    `fix(skills): move github-tool-preference seam to immediately before git fetch`. All
    other checks passed on first pass: frontmatter fields and order correct, no `Bash(*)`
    or `Bash(<cmd>:*)` patterns, `disable-model-invocation: true` present, all three
    verbatim blocks match the plan's Shared wording exactly (em-dashes U+2014, `═══` border
    characters, numeric `1.`–`8.` prefixes), section ordering matches the plan's 12-H2
    sequence, README has no stray old heading, `/sync` row is alphabetically above `/pr-review`.

    QA review (1 iteration, 0 findings): All 19 acceptance criteria from `1_spec.md` are
    addressed by the SKILL.md prose. Every behavioral requirement — dirty-tree guard with
    stash/handle prompt, re-prompt on invalid input, outside-git-repo error, ff-only failure
    handling, polyglot lockfile detection, per-ecosystem confirmation prompt, JS runner
    substitution for Node migration tools, .env key-name diff, Docker compose warning,
    project-specific instructions from CLAUDE.md/AGENTS.md, git log capped at 20 with +N
    footer, eight-section structured summary, errors-and-exits protocol — is covered. All
    non-goals from the spec are enumerated in `## What this skill will not do`.

    For Document: the PR should describe this as a developer-facing utility skill that is
    deliberately out of the `/feature` pipeline. Key design decisions worth surfacing: the
    enumerated `allowed-tools` form (not `Bash(*)`) was chosen to match the non-destructive
    contract; `Skill(github-tool-preference)` is invoked only at the two remote-operation
    seams (not at local git commands); the README section was renamed from `### PR review skill`
    to `### Standalone skills` to accommodate both skills under a single heading. No tradeoffs
    or unexpected surface changes arose during the fix — the seam reorder is a two-line move
    that doesn't affect the skill's behavior, only its documentation accuracy.

artifacts: []

recommended_skills:
  - skill: verify-coherence
    invoke_when: "After both tasks land, before the Validate step — confirm SKILL.md's structure, frontmatter, and prompt patterns mirror `pr-review/SKILL.md`, and that the README edits are internally consistent (heading rename matches the table caption, file-reference block matches the new directory, alphabetical placement is correct)."

output_artifacts: []

documentation_created: []
