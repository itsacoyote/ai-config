# context.yaml — feature workflow context
# Created by the /spec skill. Read and updated by each agent before handing off.
# To add new fields, extend this template and update agent-context/SKILL.md.

feature:
  name: "PR Review Skill"
  short_name: "pr-review"
  folder: ".docs/2026-05-29-pr-review"
  date: "2026-05-29"
  branch: "feature/pr-review"
  base_branch: "main"

workflow:
  current_step: validate  # Active step: define | research | plan | implement | validate | document | complete
  completed_steps: [define, research, plan, implement]   # Ordered list of steps that have completed and handed off
  checkpoint: ""
  escalated: false      # Set to true by an agent when it cannot resolve an issue after 3 attempts
  escalation_reason: "" # Human-readable description of what caused the escalation and what was tried
  summary: |
    Implement produced .claude/skills/pr-review/SKILL.md in six commits
    across seven tasks (Task 7 required no edits). The file is 137 lines:
    YAML frontmatter + H1 + five H2 sections. The diff is entirely additive
    — one new file, no modifications to any existing file.

    What was built. Task 1 created the directory and wrote frontmatter with
    name: pr-review, a one-sentence description, argument-hint: "[pr-number]",
    disable-model-invocation: true, and allowed-tools: Read Bash(gh *)
    Bash(git *) Agent. Tasks 2–6 filled each H2 section in workflow order.
    "What this does" opens with the Skill(github-tool-preference) invocation
    directive, states the /pr-review $ARGUMENTS invocation pattern, and
    enumerates four input-validation branches with a positive-integer regex
    guard. "Fetch the PR" specifies three ordered gh commands (repo view,
    pr view with JSON fields, pr diff) and the four data points carried
    forward. "Delegate to code-reviewer" reproduces the no-plan handoff text
    verbatim and handles the approval-string / no-findings branch. "Triage
    findings" includes the numbered overview, one-by-one default with
    batch opt-in, a six-row accepted-inputs table (keep/drop/edit/show
    checklist/go to N/anything else), batch mode, comment formatting with
    em-dash and bold path:line prefix, a state section confirming
    checklist-in-conversation-only, and the Post step with the
    printf/--body-file posting command and per-finding error handling.
    "What this skill will not do" names gh pr review --approve and
    gh pr review --request-changes by exact flag, lists eight additional
    hard constraints, and closes with the Refusal paragraph.

    Key decisions made during implementation. No deviations from the plan.
    The no-plan handoff text was reproduced verbatim as specified. The
    printf '%s' ... | gh pr comment --body-file - form was used exclusively
    in the Post step. The accepted-inputs table is a literal markdown table
    as the plan required. Task 7's AC walk confirmed all thirteen criteria
    were satisfied without any tightening edits needed.

    Context for Validate. The single file to review is
    .claude/skills/pr-review/SKILL.md. The Validate agent should check: (1)
    frontmatter parses as valid YAML with the five expected keys; (2) the
    five H2 sections appear in the specified order with no placeholders;
    (3) both forbidden commands are named by exact flag in the Non-Goals
    section; (4) the post command uses --body-file not --body; (5) the
    affirmative list is exactly yes/post/ship it; (6) no gh pr review
    appears anywhere as an instruction to run. The file contains no code,
    no tests, and no external dependencies — correctness is entirely
    textual and structural.

artifacts: []

recommended_skills: []

output_artifacts: []

documentation_created: []
