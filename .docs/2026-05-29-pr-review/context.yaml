# context.yaml — feature workflow context
# Created by the /spec skill. Read and updated by each agent before handing off.
# To add new fields, extend this template and update agent-context/SKILL.md.

feature:
  name: "PR Review Skill"
  short_name: "pr-review"
  folder: ".docs/2026-05-29-pr-review"
  date: "2026-05-29"
  branch: "feature/pr-review"
  base_branch: "main"

workflow:
  current_step: document  # Active step: define | research | plan | implement | validate | document | complete
  completed_steps: [define, research, plan, implement, validate]   # Ordered list of steps that have completed and handed off
  checkpoint: ""
  escalated: false      # Set to true by an agent when it cannot resolve an issue after 3 attempts
  escalation_reason: "" # Human-readable description of what caused the escalation and what was tried
  summary: |
    Validate approved .claude/skills/pr-review/SKILL.md on the first pass
    with zero findings. Senior-review verdict: Approved, 1 iteration. QA
    verdict: Approved, 13/13 acceptance criteria satisfied, 1 iteration.
    E2E is not applicable — the deliverable is a prose instruction document
    with no executable code or external dependencies.

    All six structural checks specified in the Implement handoff passed
    without requiring any edits: (1) frontmatter has the five expected YAML
    keys (name, description, argument-hint, disable-model-invocation,
    allowed-tools); (2) the five H2 sections appear in the correct order
    with no placeholders; (3) gh pr review --approve and
    gh pr review --request-changes are both named by exact flag in the
    Non-Goals section; (4) the post command uses --body-file exclusively;
    (5) the affirmative list is exactly `yes`, `post`, `ship it`; (6)
    `gh pr review` appears only as named prohibitions, never as an
    instruction to run. No fixes were applied — the file was complete and
    correct as delivered by Implement.

    Context for Document. The single deliverable is
    .claude/skills/pr-review/SKILL.md (136 lines). The feature is purely
    additive — one new file, no modifications to any existing file. There
    are no tradeoffs or unexpected surface changes to call out. The PR
    description should emphasize: the human-only-verdict policy baked into
    the Non-Goals and Refusal sections; the explicit post-now affirmative
    gate before any gh pr comment executes; and the session-integrity
    guarantee (checklist is conversation-only, nothing posted on abandon).
    The Document agent can derive all detail from 4_validate.md and the
    SKILL.md itself.

artifacts: []

recommended_skills: []

output_artifacts: []

documentation_created: []
