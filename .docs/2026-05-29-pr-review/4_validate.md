# Validation: PR Review Skill

**Date:** 2026-05-29
**Spec:** [1_spec.md](1_spec.md)

## Senior Code Review

**Verdict:** Approved
**Iterations:** 1

### Findings and fixes

No findings. All six structural checks passed on the first pass:

1. Frontmatter parses as valid YAML — five expected keys present: `name`, `description`, `argument-hint`, `disable-model-invocation`, `allowed-tools`.
2. Five H2 sections appear in the specified order: "What this does", "Fetch the PR", "Delegate to code-reviewer", "Triage findings", "What this skill will not do" — no placeholders.
3. Both forbidden commands named by exact flag in the Non-Goals section: `gh pr review --approve` (line 123) and `gh pr review --request-changes` (line 124).
4. Post command uses `--body-file` not `--body` (line 113); the `--body` form is explicitly prohibited in the same paragraph (line 115).
5. Affirmative list is exactly `` `yes` ``, `` `post` ``, `` `ship it` `` (line 110 and line 127).
6. `gh pr review` appears only in the Non-Goals prohibition list (lines 123–124) — never as an instruction to run.

## QA Review

**Verdict:** Approved
**Coverage achieved:** 100% (13/13 acceptance criteria)
**Iterations:** 1

### Findings and fixes

No findings. All 13 acceptance criteria from `1_spec.md` are satisfied:

- AC 1: File exists with valid frontmatter (`name: pr-review`, description, `argument-hint: "[pr-number]"`, `disable-model-invocation: true`, `allowed-tools: Read Bash(gh *) Bash(git *) Agent`). PASS.
- AC 2: `/pr-review 1250` path: fetch via `gh` (lines 27–32), delegate to `code-reviewer` (lines 36–44), numbered overview (lines 54–59). PASS.
- AC 3: Empty argument → prompts "Which PR number do you want to review?" and waits (line 19). PASS.
- AC 4: Non-numeric argument → stops with error, no `gh` call, no agent invocation (line 20). PASS.
- AC 5: Nonexistent PR → `gh pr view` non-zero exit, prints stderr, stops without agent (line 21). PASS.
- AC 6: Per-finding triage with `keep` / `drop` / `edit` options and running checklist (lines 65–78). PASS.
- AC 7: `show checklist` prints kept, dropped, remaining at any time (line 76). PASS.
- AC 8: No `gh pr comment` before explicit "post now" confirmation (lines 106–110, 127). PASS.
- AC 9: On affirmative, only kept findings posted one per comment via `gh pr comment` (lines 112–116). PASS.
- AC 10: Approve request refused; no `gh pr review --approve` called (lines 123, 136). PASS.
- AC 11: Request-changes request refused; no `gh pr review --request-changes` called (lines 124, 136). PASS.
- AC 12: Session abandonment before post step → nothing posted; checklist is conversation-only (line 102). PASS.
- AC 13: Posted comments contain no AI attribution, `Co-Authored-By`, or "Generated by" text (line 129). PASS.

## E2E Test Run

**Command:** not configured
**Result:** not configured
**Fix iterations:** 0

This skill is a prose instruction document with no executable code, no tests, and no external dependencies. Correctness is entirely textual and structural; E2E automation is not applicable.

## Evidence

The single output artifact is `.claude/skills/pr-review/SKILL.md`. It demonstrates:

- **User story — structured findings from a PR number:** The "Fetch the PR" and "Delegate to code-reviewer" sections implement `/pr-review 1250` → `gh` fetch → `code-reviewer` agent → numbered findings list.
- **User story — per-finding keep/drop triage:** The "Triage findings" section with the six-row accepted-inputs table implements the one-by-one keep/drop/edit flow with `show checklist` and `go to N` navigation.
- **User story — explicit post-now confirmation:** The "Post step" sub-section implements the final checklist summary and `yes` / `post` / `ship it` affirmative gate before any `gh pr comment` executes.
- **User story — formal-verdict refusal:** The "What this skill will not do" section and "Refusal" paragraph implement the `gh pr review --approve` / `gh pr review --request-changes` prohibitions and the human-only-verdict response script.
- **User story — session abandonment safety:** Lines 102 and 128 confirm the checklist is conversation-only and nothing is posted if the session ends before the post step.
