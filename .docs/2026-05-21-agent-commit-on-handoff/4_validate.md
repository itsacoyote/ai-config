# Validation: Agent Commit on Handoff

**Date:** 2026-05-21
**Spec:** [1_spec.md](1_spec.md)

## Senior Code Review

**Verdict:** Approved
**Iterations:** 1

### Findings and fixes

No issues found. All seven files (six agents + orchestrator skill) implement the requirements correctly on the first pass.

## QA Review

**Verdict:** Approved
**Coverage achieved:** N/A (instruction-prose feature; no automated test suite)
**Iterations:** 1

### Findings and fixes

No issues found. All twelve acceptance criteria were verified by file inspection.

## Evidence

No `output_artifacts` were registered in `context.yaml` for this feature. The implementation is entirely instruction-prose changes inside `.claude/` — no screenshots, recordings, or runtime artifacts apply.

---

### Acceptance Criteria Verification

| AC | File | Result |
|---|---|---|
| 1 — Define commit step before push | `.claude/agents/define.md` | Step 2 stages `1_spec.md` + `context.yaml` with explicit paths and commit message `docs(spec): add spec for <feature.name from context.yaml>`. Appears before step 3 (`git push -u origin`). |
| 2 — Research commit + push step | `.claude/agents/research.md` | Step 3 stages `2_research.md`, `artifacts/`, and `context.yaml` (with conditional artifacts omission). Commit message `docs(research): add research for <feature.name from context.yaml>`. Step 4 pushes. |
| 3 — Plan commit + push (no duplicate) | `.claude/agents/plan.md` | Step 3 stages `3_plan.md` + `context.yaml` together. No leftover "Commit the plan" instruction. Commit message `docs(plan): add implementation plan for <feature.name from context.yaml>`. Step 4 pushes. |
| 4 — Implement conditional commit + push | `.claude/agents/implement.md` | New "After all tasks complete" section. `git status --porcelain` guards the commit. Push is unconditional. Commit message `chore(context): update workflow checkpoint`. |
| 5 — Validate commit + push | `.claude/agents/validate.md` | Commit + push instructions follow the `4_validate.md` template block. Commit message `docs(validate): add validation report for <feature.name from context.yaml>`. |
| 6 — Document commit + push before PR ops | `.claude/agents/document.md` | New "Commit and push documentation" section between "Documentation Audit" and "PR Description". Commit message `docs: update documentation for <feature.name from context.yaml>`. |
| 7 — Orchestrator post-return commit + push | `.claude/skills/feature/SKILL.md` | Steps 6 and 7 added to post-return protocol. Commit message `chore(context): advance workflow to <next step>`. Orchestrator halts itself on push failure (announce-and-halt, not escalation YAML). |
| 8 — All new commit instructions reference git-commit skill | All seven files | Every new commit step includes "Invoke `Skill(git-commit)` first". `git-commit` is in the frontmatter `skills:` array of Research, Validate, and Document (the three that were missing it). |
| 9 — No AI attribution; Conventional Commits | All seven files | No `Co-Authored-By`, `Generated by`, emoji, Claude, or Anthropic in any new commit message string. All messages are lowercase, imperative, no trailing period. |
| 10 — No `git add -A` or `git add .` | All seven files | Only explicit paths appear in `git add` commands. Prohibition is also stated in prose in each agent. |
| 11 — Push-failure escalation in every agent | All six agent files | Each agent file contains a "Push-failure escalation" section with `workflow.escalated: true`, `workflow.escalation_reason`, the `# Merge into existing workflow block — do not replace other fields` comment, and "Do not notify the user directly." |
| 12 — Per-step commits guaranteed | Verified by instruction reading | Define: `docs(spec)`, orchestrator: `chore(context): advance workflow to research`, Research: `docs(research)`, orchestrator: `chore(context): advance workflow to plan`, Plan: `docs(plan)`, orchestrator: `chore(context): advance workflow to implement`, Implement: N task commits + 0–1 `chore(context): update workflow checkpoint`, orchestrator: `chore(context): advance workflow to validate`, Validate: `docs(validate)`, orchestrator: `chore(context): advance workflow to document`, Document: `docs`, orchestrator: `chore(context): advance workflow to complete`. At least one commit per pipeline step is guaranteed. |
