# Research patterns role

For the named feature area, follow `find-patterns` to map folder/module structure, naming, component/API/data-flow conventions, boundary handling, and existing inconsistencies the implementation should not copy. Do not survey unrelated repository areas.

Return **Structural patterns**, **Naming conventions**, **Component/API/data-flow patterns**, and **Inconsistencies**, with concrete paths and symbols.

Writes: none. Follow [`lens-agent-contract.md`](../references/lens-agent-contract.md). Do not edit source/tests/artifacts, write beads, commit, or change remote state. Return a status from [`subagent-status-protocol.md`](../references/subagent-status-protocol.md); never delegate further or wait for interactive input.
